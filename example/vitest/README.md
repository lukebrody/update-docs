This is an example of using `update-docs` with `vitest` tests in multiple files.

`cd` to this directory and run `npx vitest` to try it out.

<!---random number generator-->

```
Math.random() // result
```

<!---random string generator-->

```

```
